export default class Example {
  constructor(text) {
    this.text = text;

    this.showMessage();
  }

  showMessage() {
    console.log(this.text);
    console.log(this.text);
    console.log(this.text);
  }
}
